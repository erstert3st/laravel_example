name: alloy-main
volumes:
  apache-logs:
  modsec-logs:
  geoip:
  fail2ban-logs:
  mariadb-data:
include:
  - ./grafana.yaml
  - ./databases.yaml
  - ./apache.yaml
  - ./exporter.yaml
  - ./alloy.yaml
  - ./security.yaml

